import sys
import time
import os
import linda

linda.connect()

t = []
start = time.time()
for i in range(1000):
    t.append(linda.Type("t :: int + %i;" % (int(sys.argv[1]), )))

print "%i\t%f" % (int(sys.argv[1]), time.time()-start, )
